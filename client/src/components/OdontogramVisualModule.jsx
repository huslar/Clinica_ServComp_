import React from "react";
import { OdontogramModule } from "./OdontogramModule";

export function OdontogramVisualModule({ onNotify }) {
  return <OdontogramModule onNotify={onNotify} visualMode />;
}
