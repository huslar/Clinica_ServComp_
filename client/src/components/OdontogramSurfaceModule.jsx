import React from "react";
import { OdontogramModule } from "./OdontogramModule";

export function OdontogramSurfaceModule({ onNotify }) {
  return <OdontogramModule onNotify={onNotify} surfaceMode />;
}
